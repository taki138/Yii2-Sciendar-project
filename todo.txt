-09.02.2016---------------------------------------------------------

CREATE TABLE IF NOT EXISTS `article` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(255),
  `text` TEXT,
  `created_at` INT UNSIGNED,
  `updated_at` INT UNSIGNED,
  `user_id` INT NOT NULL,
  `description` VARCHAR(255),
  `preview_text` TEXT,
  `preview_image` VARCHAR(255),
  PRIMARY KEY `pk_id`(`id`),

  INDEX `fk_article_user_idx` (`user_id` ASC),
  CONSTRAINT `fk_article_user`
    FOREIGN KEY (`user_id`)
    REFERENCES `user` (`id`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE
) ENGINE = InnoDB;

ALTER TABLE `user` ADD COLUMN `role` TINYINT NOT NULL AFTER `status`;

ALTER TABLE `article` ADD COLUMN `status` TINYINT NOT NULL;
ALTER TABLE `article` ADD UNIQUE `preview_image_idx`(`preview_image` ASC);



-02.02.2016---------------------------------------------------------
+ Переместить файл frontend\models\Feedback.php в common\models\Feedback.php
+ В файле common\models\Feedback.php сверху заменить "namespace frontend\models" на "namespace common\models"
+ Сгенерировать CRUD
	Model Class: common\models\Feedback
	Search Model Class: backend\models\FeedbackSearch
	Controller Class: backend\controllers\FeedbackController
	View Path: @app/views/feedback


Удалить колонку email
ALTER TABLE `feedback` DROP COLUMN `email`;


-21.01.2016---------------------------------------------------------

1)
ALTER TABLE `feedback` ADD COLUMN `user_id` INT AFTER `id`;
ALTER TABLE `feedback` ADD INDEX `index_user_id`(`user_id` ASC);

2) Контроллер
Yii::$app->user->isGuest
Yii::$app->user->id

3) Комментируем валидацию email в модели
4) Комментируем поле ввода email в шаблоне
5) Вместо email в шаблоне выводим user_id

----------------------------------------------------------


CREATE DATABASE `db_name` DEFAULT CHARACTER SET utf8 COLLATE utf8_unicode_ci;

1)
ALTER TABLE `feedback` ADD COLUMN `email` VARCHAR(255) NOT NULL;

2)  форме добавить поле email

3) Валидация для email. Валидаторы: required, email

4) вывод email возле даты



